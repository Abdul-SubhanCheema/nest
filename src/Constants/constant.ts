export const connection: connectionType = {
  Connection_String: 'mongodb://localhost:27017',
  DB_Name: 'nest-fundamentals',
  DB: 'nest-fundamentals',
};

export type connectionType = {
  Connection_String: string;
  DB_Name: string;
  DB: string;
};
